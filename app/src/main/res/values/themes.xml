<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme.  This theme is only used by the system for the
         AndroidManifest and is intentionally kept simple because our UI is
         defined completely in Jetpack Compose. -->
    <style name="Theme.Pasteleria1000Sabores" parent="Theme.Material3.Light.NoActionBar">
        <!-- Primary brand colour.  These values should match those in colors.xml. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSecondary">@color/colorSecondary</item>
        <item name="colorOnSecondary">@color/colorOnSecondary</item>
        <item name="colorError">@color/colorError</item>
        <item name="colorOnError">@color/colorOnError</item>
        <!-- We disable the default action bar since Compose provides its own. -->
        <item name="android:windowActionBar">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>
</resources>